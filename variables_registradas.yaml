---
- name: Prueba con variables registradas
  hosts: centos

  tasks:
  - name: Capturar fecha
    shell:
     cmd: date
    register: fecha

  - name: Visualizar fecha
    ansible.builtin.debug:
      var: fecha

  - name: Visualizar fecha
    ansible.builtin.debug:
      msg: "{{fecha.stdout}}"